เปิด database 
```markdown
docker-compose up -d
```
ติดตั้ง go air ด้วย ถ้ายังไม่มี (ทำงานคล้าย node mon)
```markdown
go install github.com/cosmtrek/air@latest
```
run server ด้วย
```markdown
air
```
